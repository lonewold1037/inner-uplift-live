<%= turbo_stream_from @reflection %>

<script>
  console.log('🔌 Subscribing to reflection:', <%= @reflection.id %>);
  document.addEventListener('turbo:before-stream-render', (event) => {
    console.log('📥 Turbo Stream received!', event.detail);
  });
</script>

<div style="min-height: 100vh; display: flex; flex-direction: column; align-items: center; justify-content: flex-start; padding: 2rem 1rem 4rem;">
  <div id="reflection_status_area_<%= @reflection.id %>" style="width: 100%; max-width: 680px;">
    <%= render "status_content", reflection: @reflection, soundscapes: @soundscapes %>
  </div>
</div>